{
    "name": "Base Template With PostgreSQL",
    "dockerFile": "docker-compose.yml",
    "postCreateCommand": "sudo apt-get update && sudo apt-get install -y postgresql postgresql-contrib",
    "forwardPorts": [5432],
    "remoteUser": "vscode"
}